export const errorSelector = (state) => state.authorization.error;
export const loaderSelector = (state) => state.authorization.isLoading;
export const isAuthSelector = (state) => state.authorization.tokens;
